---
description: 简化企业敏捷工作流，PRD → Architecture → Development → Review 四阶段，带 2 个审批门禁。
---

简化企业敏捷工作流，PRD → Architecture → Development → Review 四阶段，带 2 个审批门禁。

## 核心约束
- 中文面向用户；与工具交互使用英文
- 每次回复包含阶段标识：`[Phase X: 阶段名]`
- 仅用 `cask/gask` 与 `cask-w/gask-w`，禁用 codeagent-wrapper

## 角色映射（6 代理 → 3 角色）

| 原始代理 | ccb 角色 | 调用方式 |
|---------|---------|---------|
| PO | Claude | 直接处理 |
| Orchestrator | Claude | 直接处理 |
| Architect | Codex | `cask-w` |
| Dev | Codex | `cask-w` |
| QA | Gemini | `gask-w` |
| UI Specialist | Gemini | `gask-w` |

## Pre-Phase: 上下文检索
使用 `acemcp` 检索相关代码上下文后再进入 Phase 1（查询使用英文）。

## 四阶段流程（2 个门禁）

### [Phase 1: PRD] 产品需求
**产物：PRD 最小模板**
- 目标与范围
- 用户/场景
- 需求列表（含优先级）
- 成功标准与验收条件

**动作：** 请求需求分析（将 `{feature}` 替换为实际功能名）：
```
cask-w "Analyze PRD for {feature}. Identify missing requirements, edge cases, and acceptance criteria gaps."
gask-w "Review PRD for {feature} from QA perspective. Suggest testability improvements and risk areas."
```

**🛑 门禁 #1：** 用户批准 PRD 后进入 Phase 2。
```
产品需求已明确。是否继续进行系统架构设计？(yes/no)
```

### [Phase 2: Architecture] 系统架构
**产物：架构摘要**
- 系统边界与主要组件
- 关键接口/数据流
- 技术选型与理由
- 风险与权衡

**动作：** 请求架构评审：
```
cask-w "Review the proposed architecture for boundary clarity, integration risks, and missing components."
```

**🛑 门禁 #2：** 用户批准架构后进入 Phase 3。
```
系统架构设计完成。是否开始实施阶段？(yes/no)
```

### [Phase 3: Development] 开发实施
**产物：执行计划 + 变更清单**
- 3-7 步实施计划
- 文件/模块/意图
- Sprint 划分（如需要）

**动作：** 按计划执行最小改动，Claude 为唯一实现者。

### [Phase 4: Review] 评审与 QA
**产物：评审结论**
- 发现的问题/风险
- 测试建议与覆盖点
- 验收状态

**动作：** 并行评审：
```
cask-w "Conduct code review. Check for correctness, security, and maintainability issues."
gask-w "Review from QA perspective. Identify test gaps and propose QA focus areas."
```

## 产物存储
产物默认存储在当前项目目录下：
```
.claude/specs/{feature_name}/
├── 01-prd.md           # PRD
├── 02-architecture.md  # 架构
├── 03-dev-plan.md      # 开发计划
└── 04-review.md        # 评审报告
```

## 输出格式
每次回复以阶段标识开头，门禁处必须等待用户明确确认。

## 错误处理
- **不确定信息：** 必须向用户询问，禁止假设
- **门禁未通过：** 返回上一阶段继续迭代
- **任务失败：** 重试一次，仍失败则报告用户

用户需求：$ARGUMENTS
